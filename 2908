#include <stdio.h>

main() {
	int A, B;
	int a, b;
	scanf("%d %d", &A, &B);
	
	a = A / 100 + A % 10 * 100 + A % 100 / 10 * 10;
	b = B / 100 + B % 10 * 100 + B % 100 / 10 * 10;

	if (a > b)
		printf("%d", a);
	else
		printf("%d", b);
}
