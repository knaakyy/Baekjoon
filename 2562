#include <stdio.h>
main(){
	int num, i;
    int max = 0;
    int cnt = 0;
	for (i = 1; i < 10; ++i)
	{
		scanf("%d", &num);
		if (num > max) {
			max = num;
			cnt = i;
		}
	}
	printf("%d\n", max);
	printf("%d", cnt);
    return 0;
}
