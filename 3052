#include <stdio.h>

int main() {
	int number[10];
	int rest[10];
	int result = 0;

	for (int i = 0; i < 10; i++) {
		scanf("%d", &number[i]);
		rest[i] = number[i] % 42;
	}
	for (int i = 0; i < 10; i++) {
		int cnt = 0;
		for (int j = i + 1; j < 10; j++) {
			if (rest[i] == rest[j]) {
				cnt++;
			}
		}
		if (cnt == 0)
			result++;
	}
	printf("%d", result);
	return 0;
}
